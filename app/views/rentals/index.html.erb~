<h1>Listing Rentals</h1>

<table>
  <thead>
    <tr>
      <th>Price</th>
      <th>Property type</th>
      <th>No of bedrooms</th>
      <th>Bedroom type</th>
      <th>No of bathrooms</th>
      <th>Bathroom type</th>
      <th>Deposit</th>
      <th>Maximum no of people per room</th>
      <th>Address</th>
      <th>Contact number</th>
      <th>Owner</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rentals.each do |rental| %>
      <tr>
				<td><%= image tag rental.image.url(:medium) %></td>
        <td><%= rental.price %></td>
        <td><%= rental.property_type %></td>
        <td><%= rental.no_of_bedrooms %></td>
        <td><%= rental.bedroom_type %></td>
        <td><%= rental.no_of_bathrooms %></td>
        <td><%= rental.bathroom_type %></td>
        <td><%= rental.deposit %></td>
        <td><%= rental.maximum_no_of_people_per_room %></td>
        <td><%= rental.address %></td>
        <td><%= rental.contact_number %></td>
        <td><%= rental.owner %></td>
        <td><%= link_to 'Show', rental %></td>
        <td><%= link_to 'Edit', edit_rental_path(rental) %></td>
        <td><%= link_to 'Destroy', rental, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rental', new_rental_path %>
